import { SocialHubScreen } from '@/features/social/social-hub-screen';

/** Always-visible Social tab for app-level friends. */
export default function SocialScreen() {
  return <SocialHubScreen />;
}
