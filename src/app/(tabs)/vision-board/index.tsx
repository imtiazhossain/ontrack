import { VisionBoardConsolidated } from '@/features/vision-board/vision-board-consolidated';

/** Primary carousel section for the vision board. */
export default function VisionBoardRoute() {
  return <VisionBoardConsolidated />;
}
